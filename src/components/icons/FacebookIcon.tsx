import styled from "@emotion/styled";

import { IconBase } from "./IconBase";

import icon from "@/content/icons/FacebookIcon.svg";

export const FacebookIcon = styled(IconBase)({
  content: `url(${icon})`,
});
