import styled from "@emotion/styled";

import { IconBase } from "./IconBase";

import icon from "@/content/icons/AlternateEmailIcon.svg";

export const AlternateEmailIcon = styled(IconBase)({
  content: `url(${icon})`,
});
