import styled from "@emotion/styled";

import { IconBase } from "./IconBase";

import icon from "@/content/icons/InstagramIcon.png";

export const InstagramIcon = styled(IconBase)({
  content: `url(${icon})`,
});
