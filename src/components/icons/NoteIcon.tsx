import styled from "@emotion/styled";

import { IconBase } from "./IconBase";

import icon from "@/content/icons/NoteIcon.svg";

export const NoteIcon = styled(IconBase)({
  content: `url(${icon})`,
});
