import styled from "@emotion/styled";

import { IconBase } from "./IconBase";

import icon from "@/content/icons/NpmIcon.svg";

export const NpmIcon = styled(IconBase)({
  content: `url(${icon})`,
});
