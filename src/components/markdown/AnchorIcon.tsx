import styled from "@emotion/styled";
import { LinkRounded as LinkIcon } from "@mui/icons-material";

export const AnchorIcon = styled(LinkIcon)(({ theme }) => ({
  paddingLeft: theme.spacing(0.5),
}));
